spring:
  application:
    name: spring-ai-web
  ai:
    model: google-genai
    google:
      genai:
        api-key: ${GEMINI_API_KEY}
        chat:
          options:
            model: gemini-2.5-flash
